{
  "name": "kruizly",
  "version": "1.0.0",
  "private": true,
  "description": "KRUIZLY — premium self-drive car rental platform. Static frontend at the repo root; the real Node backend (media storage + payment verification) lives in server/. This root package.json has no dependencies of its own — it only proxies install/start/dev to server/ so `npm run install:server && npm start` from the repo root works without anyone needing to know the backend is nested.",
  "scripts": {
    "install:server": "npm --prefix server install",
    "start": "npm --prefix server start",
    "dev": "npm --prefix server run dev"
  },
  "engines": {
    "node": ">=20"
  }
}
